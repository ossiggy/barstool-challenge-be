const { create, update, returnUpdated } = require('./nbaService');

module.exports = {
  nbaService: {
    create,
    update,
    returnUpdated
  },
  mlbService: {
    create,
    update,
    returnUpdated
  },
};
